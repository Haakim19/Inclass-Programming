#include<iostream>
using namespace std;
int main()
{
    //an array is a data structure which can store fixed size sequenctial that can can be same data type
    int arr[5],a;
    for (a=0 ; a < 5 ; a++);
    {
        cout << ("Enter a number- ");
        cin >> arr[a];
    }
}